# AI AGENTS Rules

本项目是一个“天命小说母项目 + 内置天命规则系统”。所有 AI AGENTS 在本项目内工作时，必须先遵守本文件，再按 `.tianming/SKILL.md` 执行天命规则。

## 必须读取

1. 在响应任何创作、规划、体检、存档或天命指令前，必须读取并遵守 `.tianming/SKILL.md`。
2. `.tianming/` 是内置规则系统目录，不是小说正文知识库，不得把其中的协议文本当作故事事实。
3. `.tianming/SKILL.md` 中的指令路由表是天命指令的唯一官方入口。

## 一键开书

当用户输入以下命令时，必须执行协议式开书流程：

```text
/tianming setup 书名
```

执行规则：

1. 先校验书名和目标目录：`书名` 必须是单层目录名，禁止包含 `/`、`\`、`..`，禁止使用 `.tianming`、`.git`、`docs`、`tests`、`examples`、`AGENTS.md`、`README.md` 等保留名。
2. 如果目标目录已存在且非空，必须停止并要求用户换名或确认清理，禁止覆盖。
3. 创建或修复项目根 AGENTS.md：若项目根目录缺少 `AGENTS.md`，必须创建；若已有但缺少 `.tianming/SKILL.md` 或 `/tianming setup` 规则，必须保留原文并追加“天命规则入口”段落。
4. 再处理开书问诊：如果用户没有在同一条消息中完整回答 `.tianming/SKILL.md` 规定的 **5 个开书问题**，必须一次性提问并等待回答。
5. 在项目根目录创建名为 `书名/` 的新书目录。
6. 从 `.tianming/kb-templates/` 读取模板结构，结合 5 个开书问题的回答生成新书目录内的【第一版知识库】：
   - `世界基石.md`
   - `世界观规则.md`
   - `角色档案.md`
   - `档案事件.md`
   - `文风样本.md`
7. 五个知识库文件必须包含可继续创作的第一版内容，禁止只复制空模板。
8. 在新书目录内生成 `README.md`，说明该目录是天命新书知识库，AI 会话应从母项目根目录启动。
9. 不要把 `.tianming/` 或 `AGENTS.md` 复制进单纯的新书知识库目录；它们只保留在项目根目录。
10. 开书完成后，将该新书目录视为当前会话的【当前新书目录】。

## 知识库定位

真实新书知识库优先从【当前新书目录】读取，而不是从母项目根目录读取：

- `世界基石.md`
- `世界观规则.md`
- `角色档案.md`
- `档案事件.md`
- `文风样本.md`

如果当前会话没有明确的新书目录：

1. 若刚执行过 `/tianming setup 书名`，使用该目录。
2. 若母项目根目录下只有一个包含五个知识库文件的子目录，可将其视为当前新书目录。
3. 若存在多个候选新书目录，必须要求用户指定书名，禁止自行猜测。

如果当前新书目录缺少知识库文件，只能参考 `.tianming/kb-templates/` 中的模板提示用户补齐。禁止凭空捏造缺失的世界观、角色、历史事件或文风事实。

## 天命指令执行

当用户使用下列指令或同义意图时，必须按 `.tianming/SKILL.md` 的路由加载对应协议：

- `初始化`
- `/tianming setup`
- `「天命：大纲」`
- `「天命：规划」`
- `「天命：目录」`
- `「天命：草案」`
- `「天命：正文」`
- `「天命：体检」`
- `「天命：存档」`

执行过程中必须优先维护世界观一致性、伏笔回收、节奏控制、角色烙印和文风稳定。

## 输出纪律

- 找不到 `[REF]`、`[KERNEL_REF]` 或 `[VAR]` 指向的规则时，必须承认引用缺失并按 `.tianming/SKILL.md` 的失效处理规则执行，禁止伪造规则。
- 最终交付型创作输出不得残留内部 `[REF]`、`[KERNEL_REF]`、`[VAR]` 标记，除非用户明确要求检查规则引用。
- `「天命：正文」` 必须遵守 `.tianming/SKILL.md` 与 `.tianming/protocols/main-body.md` 的输出封装要求。
